apiVersion: v1
kind: Service
metadata:
  name: {{ include "adomi-platform-api.fullname" . }}
  labels:
    {{- include "adomi-platform-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adomi-platform-api.selectorLabels" . | nindent 4 }}
